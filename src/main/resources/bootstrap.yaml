spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        shared-configs:
          - data-id: rocketMQ.yaml
            refresh: true
      discovery:
        server-addr: 127.0.0.1:8848
  application:
    name: springCloudStream
server:
  port: 8090
## 开启熔断
feign:
  hystrix:
    enabled: true